@model Frequency
<div class="awe-il">
    <button class="awe-btn" onclick="@(Url.Awe().PopupFormAction().Url(Url.Action("Edit", "Frequency", new { Model.Id })).Success("refreshFrequencyGrid").Height(300))">
        <span class="ico-edit"></span>
    </button>
</div>&nbsp;
<div class="awe-il">
@Html.Partial("deletebtn", new DelBtn { Id = Model.Id, IsDeleted = Model.IsDeleted, Controller = "Frequency" })
</div>