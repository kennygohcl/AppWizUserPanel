@model Transaction
<div class="awe-il">
    <button class="awe-btn" onclick="@(Url.Awe().PopupFormAction().Url(Url.Action("Edit", "Transaction", new { Model.Id })).Success("refreshTransactionGrid").Height(300))">
        <span class="ico-edit"></span>
    </button>
</div>&nbsp;
<div class="awe-il">
@Html.Partial("deletebtn", new DelBtn { Id = Model.Id, IsDeleted = Model.IsDeleted, Controller = "Transaction" })
</div>